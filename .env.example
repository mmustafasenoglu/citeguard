# Required for claim extraction, claim-source matching, and web-search fallback.
ANTHROPIC_API_KEY=

# Optional. citeguard can use Semantic Scholar without a key, subject to public limits.
SEMANTIC_SCHOLAR_API_KEY=
